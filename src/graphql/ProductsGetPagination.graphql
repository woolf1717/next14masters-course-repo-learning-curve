query ProductsGetPagination($take: Int!, $skip: Int!) {
  products(take: $take, skip: $skip) {
    data {
      id
      name
      description
      categories {
        name
      }
      images {
        url
      }
      price
    }
  }
}
