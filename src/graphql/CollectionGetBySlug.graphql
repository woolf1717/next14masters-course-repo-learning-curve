query CollectionGetBySlug($slug: String!) {
  collection(slug: $slug) {
    description
    name
    products {
      id
      name
      description
      rating
      categories {
        name
      }
      images {
        url
      }
      price
    }
  }
}
