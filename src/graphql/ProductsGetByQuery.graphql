query ProductsGetByQuery($query: String!) {
  products(search: $query) {
    data {
      id
      name
      description
      categories {
        name
      }
      images {
        url
      }
      price
    }
  }
}
