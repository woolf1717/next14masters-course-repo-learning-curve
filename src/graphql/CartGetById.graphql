query CartGetById($id: ID!) {
  cart(id: $id) {
    id
    items {
      quantity
      product {
        id
        name
        images {
          url
        }
        categories {
          id
          name
        }
        price
      }
    }
  }
}
